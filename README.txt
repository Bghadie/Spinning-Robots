Name: Billal Ghadie
Instructions:

The code is straight forward to run, it takes no command line arguments. Run the environment server as a background process then run as many robot clients in the background as allowed (21). The sleep is set to 2 seconds feel free to adjust this in the robot client source file. When you are done, call the stop executable and all processes will quit 

Compile line: make
